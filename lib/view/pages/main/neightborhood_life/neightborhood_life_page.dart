import 'package:flutter/material.dart';

class NeightborhoodLifePage extends StatelessWidget {
  const NeightborhoodLifePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("chattingPage"),
    );
  }
}
